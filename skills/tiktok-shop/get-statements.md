# TikTok Shop API: Get Statements

# Get Statements API Guide

## Overview
API fetch shop statements. Use to sync finance data, check payout status, track revenue.

## Endpoint
*   **Method:** `GET`
*   **Path:** `https://open-api.tiktokglobalshop.com/finance/202309/statements`

## Headers
*   `content-type`: `application/json` (Required)
*   `x-tts-access-token`: Seller access token (Required)

## Query Parameters

| Name | Type | Required | Description |
| :--- | :--- | :--- | :--- |
| `app_key` | string | Yes | Unique app key. |
| `sign` | string | Yes | Signature generated by algorithm. |
| `timestamp` | number | Yes | Unix timestamp GMT (UTC+00:00). |
| `shop_cipher` | string | Yes | Shop identifier. |
| `sort_field` | string | Yes | Sort field. Only supports `statement_time`. |
| `sort_order` | string | No | Sort order. Default: `ASC`. Values: `ASC`, `DESC`. |
| `payment_status` | string | No | Filter. Values: `PAID`, `FAILED`, `PROCESSING`. |
| `statement_time_ge` | number | No | Start time filter. Unix timestamp. |
| `statement_time_lt` | number | No | End time filter. Unix timestamp. |
| `page_size` | number | No | Results per page. Default: 20. Range: [1-100]. |
| `page_token` | string | No | Token for next page. |

## Response Structure

*   `code`: Status code. `0` means success.
*   `message`: Status message.
*   `request_id`: Log identifier.
*   `data`: Object.
    *   `next_page_token`: Token for next page.
    *   `statements`: Array of statement objects.
        *   `id`: Statement ID.
        *   `statement_time`: Generation time (Unix timestamp).
        *   `settlement_amount`: Settlement value.
        *   `currency`: Currency code.
        *   `revenue_amount`: Revenue value.
        *   `fee_amount`: Fee value.
        *   `adjustment_amount`: Adjustment value.
        *   `payment_status`: Status.
        *   `payment_id`: Payment ID.
        *   `net_sales_amount`: Net sales value.
        *   `shipping_cost_amount`: Shipping cost.
        *   `payment_time`: Payment time (Unix timestamp).

## Error Handling
*   `36009003`: Internal error. Retry request. Contact support if failure persists.



---
*Auto-generated from TikTok Shop Partner Center documentation*
*Endpoint: `GET /finance/202309/statements`*
